"use client";

// Canvas renders the selector row and content panel on top of this via
// TabsEditHeader / TabsTryHeader — keep transparent here.
export default function TabsVisual() {
  return <div style={{ width: "100%", height: "100%", background: "transparent" }} />;
}
